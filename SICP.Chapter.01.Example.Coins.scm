;;;Beginning attempt at building a change maker

(define (get-highest-combination value)
  (if (

(define (change-iter value)
  (get-highest-combination

(define (number-of-ways-to-make-change value)
  (change-iter value))

