;;; Exercise 2.28

(define x (list (list 1 2) (list 3 4)))  ; ((1 2) (3 4))

;(define (map proc items)
;  (if (null? items)
;      items
;      (cons (proc (car items))
;            (map proc (cdr items)))))


(map (lambda (x) (* x x)) x)

(define (fringe tree